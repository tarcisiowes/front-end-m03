import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    maxWidth: 300,
    height: 'auto'  
  },
  media: {
    height: 'auto',
    width: '100%'
  },
  cardHeader: {
    textAlign: 'center'
  }
}));